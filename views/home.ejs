<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Mandate Wire</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <div class="logo">The Mandate <span class="accent">Wire</span></div>
        <nav>
            <a href="/posts" class="active">Home</a>
            <a href="/category/policy">Policy</a>
            <a href="/category/global-affairs">Global Affairs</a>
            <a href="/category/technology">Technology</a>
            <a href="/category/opinion">Opinion</a>
            <a href="/about">About</a>
        </nav>
        <div class="user-menu">
            <% if (userId) { %>
                <a href="/posts/create">Create Post</a>
                <form action="/auth/logout" method="post" style="display:inline;">
                    <button type="submit">Logout</button>
                </form>
            <% } else { %>
                <a href="/auth/login">Login</a>
                <a href="/auth/register">Register</a>
            <% } %>
        </div>
        <div class="hamburger">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </header>
    <main>
        <% if (posts.length > 0) { %>
            <article class="hero">
                <h2><a href="/posts/<%= posts[0].post_id %>"><%= posts[0].title %></a></h2>
                <p class="meta">By <%= posts[0].username %> on <%= new Date(posts[0].published_at || posts[0].created_at).toLocaleDateString() %></p>
                <p class="excerpt"><%= posts[0].content.substring(0, 300) %>...</p>
            </article>
        <% } %>

        <div class="content-wrapper">
            <div class="main-content">
                <div class="grid">
                    <% posts.slice(1, 7).forEach(post => { %>
                        <article class="article-card">
                            <h3><a href="/posts/<%= post.post_id %>"><%= post.title %></a></h3>
                            <p class="excerpt"><%= post.content.substring(0, 150) %>...</p>
                            <p class="meta"><%= post.username %> • <%= new Date(post.published_at || post.created_at).toLocaleDateString() %></p>
                        </article>
                    <% }); %>
                </div>
            </div>

            <aside class="sidebar">
                <div class="sidebar-section">
                    <h3>Trending Now</h3>
                    <ul>
                        <% posts.slice(0, 5).forEach((post, index) => { %>
                            <li><a href="/posts/<%= post.post_id %>"><%= index + 1 %>. <%= post.title %></a></li>
                        <% }); %>
                    </ul>
                </div>

                <div class="sidebar-section">
                    <h3>Newsletter</h3>
                    <p>Stay updated with the latest insights.</p>
                    <form class="newsletter-form">
                        <input type="email" placeholder="Enter your email" required>
                        <button type="submit">Subscribe</button>
                    </form>
                </div>
            </aside>
        </div>
    </main>
    <footer>
        <div class="footer-content">
            <div class="footer-section">
                <div class="footer-logo">The Mandate <span class="accent">Wire</span></div>
                <p>Delivering quality content and fostering community engagement.</p>
            </div>
            <div class="footer-section">
                <h3>Quick Links</h3>
                <ul>
                    <li><a href="/about">About</a></li>
                    <li><a href="/contact">Contact</a></li>
                    <li><a href="/privacy">Privacy</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>Follow Us</h3>
                <div class="social-icons">
                    <a href="#" aria-label="Twitter">🐦</a>
                    <a href="#" aria-label="Facebook">📘</a>
                    <a href="#" aria-label="LinkedIn">💼</a>
                </div>
            </div>
            <div class="footer-section">
                <h3>Newsletter</h3>
                <p>Stay updated with the latest insights.</p>
                <div class="footer-newsletter">
                    <input type="email" placeholder="Enter your email">
                    <button type="submit">Subscribe</button>
                </div>
            </div>
        </div>
        <p>&copy; 2025 The Mandate Wire. All rights reserved.</p>
    </footer>
</body>
</html>