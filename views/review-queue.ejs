<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Review Queue - The Mandate Wire</title>
    <link rel="icon" type="image/png" href="/tmwicon.png">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap');
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-gray-50">
    <header class="bg-slate-900 shadow-xl">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <a href="/" class="text-3xl font-extrabold tracking-tight text-white">
                    <span class="text-blue-500">The</span> Mandate Wire
                </a>
                <nav class="flex space-x-6">
                    <a href="/posts" class="text-gray-300 hover:text-blue-400 font-semibold">Home</a>
                    <a href="/posts/review-queue" class="text-yellow-400 font-semibold">Review Queue</a>
                    <a href="/posts/fact-check-queue" class="text-orange-400 font-semibold">Fact-Check Queue</a>
                    <form action="/auth/logout" method="post" style="display:inline;">
                        <button type="submit" class="text-gray-300 hover:text-blue-400 font-semibold">Logout</button>
                    </form>
                </nav>
            </div>
        </div>
    </header>

    <main class="max-w-4xl mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold text-gray-900 mb-8">Editorial Review Queue</h1>

        <% if (posts.length === 0) { %>
            <div class="text-center py-12">
                <p class="text-gray-500 text-lg">No posts waiting for review.</p>
            </div>
        <% } else { %>
            <div class="space-y-6">
                <% posts.forEach(post => { %>
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <div class="flex justify-between items-start mb-4">
                            <div>
                                <h2 class="text-xl font-bold text-gray-900 mb-2"><%= post.title %></h2>
                                <p class="text-sm text-gray-600 mb-1">Author: <%= post.username %></p>
                                <p class="text-sm text-gray-600 mb-2">Category: <%= post.category %></p>
                                <p class="text-sm text-gray-500">Submitted: <%= new Date(post.submitted_at).toLocaleDateString() %></p>
                            </div>
                            <div class="flex space-x-2">
                                <a href="/posts/<%= post.post_id %>" class="px-4 py-2 bg-blue-600 text-white text-sm rounded-md hover:bg-blue-700">
                                    View Full Post
                                </a>
                                <form action="/posts/<%= post.post_id %>/review" method="post" style="display:inline;">
                                    <input type="hidden" name="action" value="approve">
                                    <button type="submit" class="px-4 py-2 bg-green-600 text-white text-sm rounded-md hover:bg-green-700">
                                        Approve for Fact-Check
                                    </button>
                                </form>
                                <form action="/posts/<%= post.post_id %>/review" method="post" style="display:inline;">
                                    <input type="hidden" name="action" value="reject">
                                    <button type="submit" onclick="return confirm('Are you sure you want to reject this post?')" class="px-4 py-2 bg-red-600 text-white text-sm rounded-md hover:bg-red-700">
                                        Reject
                                    </button>
                                </form>
                            </div>
                        </div>
                        <div class="text-gray-700">
                            <%= post.content.substring(0, 500) %>...
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </main>
</body>
</html>